# ใบงานที่ 7.1  การศึกษากลไก Reset Provisioning 3 รูปแบบ และ NVS Memory Forensics
```
I (27) boot: ESP-IDF v6.0.2 2nd stage bootloader
I (27) boot: compile time Aug 27 2026 17:29:52
I (28) boot: Multicore bootloader
I (29) boot: chip revision: v3.1
I (32) boot.esp32: SPI Speed      : 40MHz
I (35) boot.esp32: SPI Mode       : DIO
I (39) boot.esp32: SPI Flash Size : 2MB
I (42) boot: Enabling RNG early entropy source...
I (47) boot: Partition Table:
I (49) boot: ## Label            Usage          Type ST Offset   Length
I (56) boot:  0 nvs              WiFi data        01 02 00009000 00006000
I (62) boot:  1 phy_init         RF data          01 01 0000f000 00001000
I (69) boot:  2 factory          factory app      00 00 00010000 00100000
I (75) boot: End of partition table
I (79) esp_image: segment 0: paddr=00010020 vaddr=3f400020 size=1daa4h (121508) map
I (129) esp_image: segment 1: paddr=0002dacc vaddr=3ffb0000 size=0254ch (  9548) load
I (133) esp_image: segment 2: paddr=00030020 vaddr=400d0020 size=92da0h (601504) map
I (348) esp_image: segment 3: paddr=000c2dc8 vaddr=3ffb254c size=0200ch (  8204) load
I (351) esp_image: segment 4: paddr=000c4ddc vaddr=40080000 size=1562ch ( 87596) load
I (388) esp_image: segment 5: paddr=000da410 vaddr=50000000 size=00028h (    40) load
I (399) boot: Loaded app from partition at offset 0x10000
I (399) boot: Disabling RNG early entropy source...
I (410) cpu_start: Multicore app
I (418) cpu_start: GPIO 3 and 1 are used as console UART I/O pins
I (418) cpu_start: Pro cpu start user code
I (418) cpu_start: cpu freq: 160000000 Hz
I (420) app_init: Application information:
I (424) app_init: Project name:     lab7_1_reset_nvs_forensics
I (429) app_init: App version:      1
I (433) app_init: Compile time:     Aug 27 2026 17:28:46
I (438) app_init: ELF file SHA256:  b829ced20...
I (442) app_init: ESP-IDF:          v6.0.2
I (446) efuse_init: Min chip rev:     v0.0
I (450) efuse_init: Max chip rev:     v3.99 
I (454) efuse_init: Chip rev:         v3.1
I (458) heap_init: Initializing. RAM available for dynamic allocation:
I (464) heap_init: At 3FFAE6E0 len 00001920 (6 KiB): DRAM
I (469) heap_init: At 3FFB8A80 len 00027580 (157 KiB): DRAM
I (474) heap_init: At 3FFE0440 len 00003AE0 (14 KiB): D/IRAM
I (480) heap_init: At 3FFE4350 len 0001BCB0 (111 KiB): D/IRAM
I (485) heap_init: At 4009562C len 0000A9D4 (42 KiB): IRAM
I (492) spi_flash: detected chip: generic
I (494) spi_flash: flash io: dio
W (497) spi_flash: Detected size(4096k) larger than the size in the binary image header(2048k). Using the size in the binary image header.
I (511) main_task: Started on CPU0
I (511) main_task: Calling app_main()
I (511) LAB7_1_RESET: Hold GPIO 18 button for 3 seconds to trigger Factory Reset...
I (541) wifi:wifi driver task: 3ffc0afc, prio:23, stack:6656, core=0
I (541) wifi:wifi firmware version: 00ad238
I (541) wifi:wifi certification version: v7.0
I (541) wifi:config NVS flash: enabled
I (541) wifi:config nano formatting: disabled
I (551) wifi:Init data frame dynamic rx buffer num: 32
I (551) wifi:Init static rx mgmt buffer num: 5
I (561) wifi:Init management short buffer num: 32
I (561) wifi:Init dynamic tx buffer num: 32
I (561) wifi:Init static rx buffer size: 1600
I (571) wifi:Init static rx buffer num: 10
I (571) wifi:Init dynamic rx buffer num: 32
I (581) wifi_init: rx ba win: 6
I (581) wifi_init: accept mbox: 6
I (581) wifi_init: tcpip mbox: 32
I (581) wifi_init: udp mbox: 6
I (591) wifi_init: tcp mbox: 6
I (591) wifi_init: tcp tx win: 5760
I (591) wifi_init: tcp rx win: 5760
I (601) wifi_init: tcp mss: 1440
I (601) wifi_init: WiFi IRAM OP enabled
I (601) wifi_init: WiFi RX IRAM OP enabled
W (611) LAB7_1_RESET: --------------------------------------------------
W (611) LAB7_1_RESET: [STATUS]: Device is NOT provisioned (NVS is empty)
W (621) LAB7_1_RESET: Ready for Provisioning Lab 7-2 (SoftAP) or 7-3 (BLE)!
W (631) LAB7_1_RESET: --------------------------------------------------
```

## 5. กิจกรรมถอดรหัสซอร์สโค้ดและเขียนผังงาน (Code Deconstruction & Flowchart Assignment)

ให้นักศึกษาศึกษาโค้ดใน `main/main.c` และ `main/led_indicator.c` แล้วเขียน **ผังงาน (Flowchart / State Diagram)** เพื่ออธิบายการตัดสินใจและการทำงานของระบบ:

### ภารกิจที่ 1  ผังงานการตัดสินใจช่วง Bootstrapping & Reset Decision
ให้นักศึกษาวาด Flowchart แสดงลำดับตรรกะการตรวจสอบเงื่อนไขตั้งแต่เริ่มต้นรันฟังก์ชัน `app_main()` โดยต้องครอบคลุม:
1. การตรวจสอบสถานะปุ่ม **GPIO 18** (ตรวจจับการกดค้าง 3 วินาที)
2. การทำงานของ `nvs_flash_init()` และกรณีที่ต้อง `nvs_flash_erase()`
3. การตรวจสอบ Macro `#ifdef CONFIG_EXAMPLE_RESET_PROVISIONED`
4. การเรียกฟังก์ชัน `wifi_prov_mgr_is_provisioned(&provisioned)`
5. จุดแยกสายการทำงานเข้าสู่โหมด **Provisioning Mode** หรือ **Station Mode**

```text
flowchart TD
    Start([เริ่มต้น app_main]) --> CheckBtn{กดปุ่ม GPIO 18<br/>ค้าง 3 วินาที?}
    
    CheckBtn -- ใช่ --> Erase1[ล้างข้อมูล NVS: nvs_flash_erase] --> InitNVS
    CheckBtn -- ไม่ใช่ --> InitNVS[เริ่มต้น NVS: nvs_flash_init]
    
    InitNVS --> CheckNVS{ret == NO_FREE_PAGES หรือ<br/>NEW_VERSION_FOUND?}
    
    CheckNVS -- ใช่ --> FixNVS[ล้างและเริ่ม NVS ใหม่] --> CheckMacro
    CheckNVS -- ไม่ใช่ --> CheckMacro
    
    CheckMacro{#ifdef CONFIG_EXAMPLE_RESET_PROVISIONED}
    CheckMacro -- มีกำหนดไว้ --> ResetProv[รีเซ็ตการตั้งค่า Wi-Fi] --> CheckProv
    CheckMacro -- ไม่ได้กำหนด --> CheckProv
    
    CheckProv[ตรวจสถานะ Wi-Fi] --> IsProv{ตั้งค่า Wi-Fi ไว้แล้วหรือไม่?}
    
    IsProv -- ใช่ --> Station[เข้าสู่ Station Mode]
    IsProv -- ไม่ใช่ --> Provisioning[เข้าสู่ Provisioning Mode]
```

<img width="1788" height="8192" alt="NVS Management Flow for App-2026-08-30-044626" src="https://github.com/user-attachments/assets/19303845-6aa3-44aa-9b3e-f019a0d3c05e" />


### ภารกิจที่ 2 ผังสถานะการเปลี่ยนจังหวะไฟ LED 1 (Wi-Fi STA Indicator)
ให้นักศึกษาวาด State Diagram แสดงการเปลี่ยนสถานะของ **LED 1 (GPIO 2)**:
- เงื่อนไขใดทำให้ LED 1 เข้าสู่สถานะ `LED_STA_MODE_DISCONNECTED` (กระพริบ 200ms Mark / 200ms Space)
- เงื่อนไขหรือ Event ใดทำให้เปลี่ยนเป็น `LED_STA_MODE_CONNECTED` (Heartbeat 200ms ทุก 1s)
```text
stateDiagram-v2
    [*] --> LED_STA_MODE_DISCONNECTED : เริ่มต้นระบบ

    LED_STA_MODE_DISCONNECTED --> LED_STA_MODE_CONNECTED : ได้รับ IP_EVENT_STA_GOT_IP\n(เชื่อมต่อ Wi-Fi สำเร็จ)
    
    LED_STA_MODE_CONNECTED --> LED_STA_MODE_DISCONNECTED : ได้รับ WIFI_EVENT_STA_DISCONNECTED\n(Wi-Fi หลุดการเชื่อมต่อ)

    state LED_STA_MODE_DISCONNECTED {
        [*] --> DisconnectedPattern
        DisconnectedPattern : กระพริบ 200ms Mark / 200ms Space
    }

    state LED_STA_MODE_CONNECTED {
        [*] --> ConnectedPattern
        ConnectedPattern : Heartbeat 200ms ทุก 1s
    }
```
<img width="600" height="auto" alt="LED Connection State Flow-2026-08-30-045852" src="https://github.com/user-attachments/assets/d4177f41-b260-4080-96a2-0ffc1949c9ac" />


---

## 6. ตารางบันทึกผลการทดลอง (Experiment Results)

| รูปแบบการ Reset                  | คำสั่ง / พฤติกรรมที่ทำ               | พฤติกรรมของ LED แต่ละดวงหลังเปิดเครื่อง | สถานะใน Serial Monitor |
| :------------------------------- | :----------------------------------- | :-------------------------------------- | :--------------------- |
| **1. CLI Erase**                 | `idf.py erase-flash`                 |   LED ทุกดวงดับสนิท ไม่มีไฟสถานะใดติด        |  ไม่มี Log ทำงาน (ชิปว่างเปล่า) จนกว่าจะ Flash เฟิร์มแวร์ใหม่                      |
| **2. Menuconfig Flag**           | `CONFIG_EXAMPLE_RESET_PROVISIONED=y` | LED 1 กระพริบถี่ต่อเนื่อง (เข้าสู่โหมด Provisioning รอรับค่า Wi-Fi ใหม่ทุกครั้งที่บูต)                    | แจ้งเตือนรีเซ็ตค่าเดิม และระบุสถานะว่ายังไม่ได้ Provision (Device is NOT provisioned)                       |
| **3. Hardware Button (GPIO 18)** | กดปุ่ม GPIO 18 ค้าง 3 วินาที         |   LED 1 เปลี่ยนเป็นกระพริบถี่ เพื่อเริ่มกระจายสัญญาณ Provisioning ใหม่                                      |   ขึ้นข้อความล้าง NVS Flash (FACTORY RESET TRIGGERED!) และสลับเข้าสู่ Provisioning Mode                     |

---

## 7. คำถามท้ายการทดลอง (Post-Lab Questions)
1. เพราะเหตุใดการกดปุ่ม BOOT (GPIO 0) ค้างไว้ในจังหวะรีเซ็ตบอร์ด จึงทำให้โปรแกรมค้างอยู่ที่ ROM Bootloader และไม่ยอมทำงานต่อ?
> ขา GPIO 0 ทำหน้าที่เป็น Strapping Pin สำหรับเลือกโหมดการบูต เมื่อกดปุ่มค้างไว้ ขา GPIO 0 จะกลายเป็นสถานะ Low (0) ทำให้ชิปเข้าสู่ UART Download Mode เพื่อรอรับการแฟลชโปรแกรมใหม่ผ่าน Serial แทนที่จะโหลดโค้ดแอปพลิเคชันขึ้นมารันตามปกติ
2. เพราะเหตุใดคำสั่ง `idf.py erase-flash` จึงทำให้ข้อมูลเฟิร์มแวร์ Application หายไปด้วย ในขณะที่ `nvs_flash_erase()` ไม่ทำให้เฟิร์มแวร์หาย?
> idf.py erase-flash: เป็นคำสั่งระดับฮาร์ดแวร์ที่ ล้างข้อมูล Flash ทั้งหมดทุกเซกเตอร์ รวมถึงโค้ดโปรแกรมหลัก (App Partition) และ Bootloader ทำให้บอร์ดว่างเปล่า nvs_flash_erase(): เป็นฟังก์ชันที่สั่ง ลบเฉพาะพาร์
3. การออกแบบปุ่ม Factory Reset บนอุปกรณ์ IoT เชิงพาณิชย์ เหตุใดจึงต้องกำหนดให้ผู้ใช้กดปุ่มค้างไว้ 3-5 วินาที แทนที่จะสั่งลบข้อมูลทันทีที่แตะปุ่มเพียงเสี้ยววินาที?
> ป้องกันการกดโดนโดยไม่ตั้งใจ: ป้องกันข้อมูลสำคัญหายจากการเผลอแตะหรือโดนกระแทกชั่วขณะ กรองสัญญาณรบกวน (Debouncing): ป้องกันไฟกระชากหรือสัญญาณรบกวนชั่วคราวสั่งล้างข้อมูลผิดพลาด ให้เวลายืนยันเจตนา: เปิดโอกาสให้อุปกรณ์แจ้งเตือนผ่านไฟ LED/เสียงก่อนล้างค่าจริง ทำให้ผู้ใช้ยกเลิกได้หากไม่ได้ตั้งใจ
4. หากอุปกรณ์ IoT ถูกติดตั้งอยู่บนเสาสูงหรือฝังอยู่ในผนัง วิธีการ Reset ทางกายภาพรูปแบบใดเหมาะสมที่สุด?
> Power-Cycle Sequence Reset: ตรวจจับการเปิด-ปิดสวิตช์ไฟหลักติดต่อกัน 3–5 ครั้ง เพื่อสั่ง Factory Reset ผ่านระบบไฟ (นิยมใช้ในหลอดไฟหรือสวิตช์อัจฉริยะ) Magnetic Reset (Hall Effect / Reed Switch): ฝังเซนเซอร์ตรวจจับสนามแม่เหล็กไว้ภายในตัวเครื่อง แล้วนำแม่เหล็กแรงสูงมาทาบจากภายนอกกล่องหรือกำแพงโดยไม่ต้องแกะเคส
